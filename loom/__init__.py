"""loom — minimal Claude-Code-like coding agent and harness tooling.

Public entry points:
- ``loom.cli:main`` — CLI for ``loom init`` and ``loom audit`` subcommands
- ``loom.detect`` — project stack detection + verification command generation
- ``loom.init_cmd`` — generate a minimum 5-file harness in a target project
"""

__version__ = "0.2.0"
