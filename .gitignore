# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

.sisyphus/

claude-code-src/

# Harness reference skill (not part of the loop product)
harness-creator/

# Phase 2: .minicode/ contains runtime data (memory, mailboxes, traces, checkpoints)
.minicode/

MEMORY.md

test.py
loop.log
loom.log
.pytest_cache/

.env

# Tooling caches
.ruff_cache/
.mypy_cache/

# Sisyphus / opencode runtime artifacts (skills registry, lockfile).
# Leading slash anchors the pattern to repo root so it doesn't
# accidentally match nested directories like loom/agent/.
.agents/
/agent/
skills-lock.json


.DS_Store
snapshot_report.html
