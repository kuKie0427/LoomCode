"""loop — minimal Claude-Code-like coding agent and harness tooling.

Public entry points:
- ``loop.cli:main`` — CLI for ``loop init`` and ``loop audit`` subcommands
- ``loop.detect`` — project stack detection + verification command generation
- ``loop.init_cmd`` — generate a minimum 5-file harness in a target project
"""

__version__ = "0.2.0"
